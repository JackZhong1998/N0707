# NowBuild 0.1.0 目录发布检查文档

## 目标

用户确认要发布的 Directory 后，NowBuild 自动完成资料检查、资料准备和逐平台后台提交。只有 AI 无法可靠生成或平台阻塞时，才要求用户介入。

核心体验：

> 选择目录 → 自动检查要求 → AI 自动补齐文本 → 向用户索取必要资料 → 后台逐个平台辅助填写 → 遇到阻塞通知用户 → 用户处理后自动续跑

当前版本不包含：下载功能、AI 生成图片、提交后的自动审核/上线跟踪、自动提交白名单验证。

## 一、提交前资料检查

- [x] 为每个 Directory 建立独立的要求清单，不能继续共用统一的“名称、网址、Logo、截图”描述。
- [ ] 要求清单至少包含：
  - 文本字段及长度限制；
  - 分类、标签和可选值；
  - Logo、截图、视频的数量、尺寸、比例、格式和大小限制；
  - 创始人、团队、联系方式和社交链接；
  - 登录、企业邮箱、验证码、付费、反向链接和资格限制；
- [x] 用户选择目录后，立即执行批量预检并展示：
  - 已具备；
  - AI 可生成；
  - 需要用户提供；
  - 平台提交时才能确认。
- [x] 检查结果按目录展示，并汇总成一份去重后的待补资料清单。
- [x] 在资料未满足最低要求时，不启动对应目录的提交任务。

## 二、AI 自动准备资料

- [x] 优先复用官网分析、Launch Brief、定位、卖点和已有提交资料。
- [x] AI 自动生成可安全推导的内容：
  - Slogan 和一句话介绍；
  - 不同长度的产品简介；
  - 分类、标签和关键词建议；
  - 面向不同目录的描述版本；
  - Logo、截图的裁剪、压缩、缩放和格式转换；
  - 平台要求的补充说明草稿。
- [ ] 生成内容必须符合目标平台的长度、格式和禁用词要求。
- [ ] 同一事实保持一致，不能为了适配平台虚构用户数、收入、融资、客户或奖项。
- [ ] 普通营销文案可自动使用；涉及身份、法律声明、商业数据或承诺的内容必须由用户确认。
- [x] 保存生成结果和来源，后续目录可复用，避免重复生成和重复询问。

## 三、向用户索取 AI 无法生成的资料

- [ ] 只询问当前提交批次真正缺少的内容。
- [ ] 合并不同目录的重复问题，一次询问、多处复用。
- [ ] 必须向用户索取或确认的内容包括：
  - 登录、验证码、2FA 和邮箱验证；
  - 创始人真实身份及私人联系方式；
  - 无法从官网取得的原始品牌素材；
  - 付费方案选择；
  - 条款、授权、资格和真实性声明；
  - 反向链接或修改官网的授权；
  - AI 无法确定的分类或产品属性。
- [ ] 每个问题说明“哪个目录需要、为什么需要、是否会公开”。
- [ ] 用户补充后自动重新检查，并继续被阻塞的任务。

## 四、后台逐平台提交

- [x] 创建持久化提交队列，每个 Directory 对应一个独立任务。
- [ ] 支持串行或限流并发，避免短时间打开大量页面或触发平台风控。
- [ ] 每个任务必须保存：
  - Directory ID、入口和适配器版本；
  - 使用的资料快照；
  - 当前步骤和状态；
  - 已填写字段、已上传素材；
  - 阻塞原因、失败日志和重试次数；
  - 提交时间、证明链接和上线链接。
- [x] 支持多页面表单、登录后续跑和中断恢复。
- [x] 使用幂等键避免刷新、重试或重复点击造成重复提交。
- [x] 自动填写停在最终提交前；涉及付费、法律确认或不可逆操作时必须暂停。
- [x] 无插件或后台能力的平台进入“需要人工提交”，并提供已准备好的完整资料。

## 五、阻塞检测与用户通知

- [ ] 自动识别以下阻塞：
  - 登录、验证码、2FA、邮箱验证；
  - 付费、绑卡或方案选择；
  - 条款、授权和真实性确认；
  - 企业邮箱、邀请制或资格限制；
  - 反向链接、徽章或修改官网；
  - 缺少资料或素材规格不符；
  - 页面改版、入口失效或字段无法识别；
  - 平台拒绝、重复提交或频率限制。
- [x] Directory 页面顶部展示统一的“需要你处理”通知区。
- [x] 通知必须包含目录名称、阻塞原因、需要完成的动作和“前往处理”按钮。
- [x] 用户完成后可点击“我已完成，继续”，系统从暂停位置续跑。
- [x] 同时保留浏览器通知能力，用户回到 NowBuild 后仍能看到未处理事项。
- [x] 一个目录被阻塞不能停止其他目录任务。

## 六、状态与页面展示

统一状态：

`待检查 → 缺少资料 → 资料准备中 → 填写中 → 需要用户操作 → 已准备 / 已提交`

异常状态：

`提交失败 / 被拒绝 / 不符合条件 / 入口失效 / 已跳过`

- [x] 页面默认优先展示下一步动作，而不是超宽数据表。
- [x] 显示整体进度：总数、资料已齐、提交中、需操作、已提交、已上线。
- [ ] 每个目录显示准确的所需资料、自动化能力、费用、限制和最后核验时间。
- [ ] 支持查看任务执行记录和阻塞记录。
- [x] 状态由后台任务自动更新；用户只在无法可靠识别时手动回填。

## 七、Directory 适配要求

- [x] 每个 Directory 使用独立配置，包含字段映射、素材规格、流程阶段、阻塞规则和提交策略。
- [x] “插件辅助”必须以插件实际返回的支持清单为准，不能仅根据前端域名配置判断。
- [ ] 每个标记为支持的 Directory 都要有真实页面测试记录和最近验证日期。
- [ ] 页面改版或字段失效时自动降级为“部分支持”或“人工提交”。
- [ ] 优先覆盖高价值且入口稳定的平台，再扩展长尾目录。

## 八、0.1.0 发布阻塞项

- [x] “插件辅助”改为读取用户当前安装插件返回的真实支持清单，不再仅根据前端静态域名配置判断。
- [x] 新增按 Directory 的真实资料要求模型。
- [x] 新增提交前批量资料检查。
- [x] 新增 AI 自动生成、转换和复用资料的流程。
- [x] 新增缺失资料统一询问流程。
- [x] 新增持久化后台提交队列和断点续跑。
- [x] 新增阻塞通知、用户接管和继续执行。
- [x] 新增幂等、防重复提交和失败重试。
- [x] 将“检查 Launch Kit”改为用户可理解的“检查提交资料”。
- [x] 将“用插件填写”按真实能力改为“自动提交”“辅助填写”或“人工提交”。

## 九、验收标准

- [ ] 用户选择一批 Directory 后，可以一次看到完整的资料缺口。
- [ ] AI 可生成的资料无需用户逐项填写。
- [ ] AI 无法生成的资料会被合并成最少数量的问题。
- [ ] 资料齐全后，系统能在后台逐个平台推进，不要求用户一直停留在页面。
- [ ] 任一平台阻塞时，用户收到明确通知，其他任务继续运行。
- [ ] 用户处理阻塞后，任务能从原位置继续，而不是重新开始。
- [ ] 不重复提交同一产品到同一 Directory。
- [ ] 页面上的自动化能力与插件真实能力完全一致。
- [ ] 每个完成任务都能追溯使用的资料、执行过程和结果证明。

## 十、安全边界

- 不保存第三方平台密码。
- 不绕过验证码、2FA 或平台访问限制。
- 不自动付费、绑卡或购买方案。
- 不在未经授权时接受法律条款、发布真实性声明或修改用户官网。
- 不伪造产品信息、商业数据、用户评价或资质。
- 所有付费和不可逆操作必须通知用户并获得明确确认。
